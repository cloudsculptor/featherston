import PageLayout from "../components/PageLayout";

export default function Transport() {
  return (
    <PageLayout pageName="Transport">
      <h2>Transport</h2>
      <p>
        Find carpooling arrangements, ride sharing, and local transport options.
        Whether you need a lift to the city or can offer one, connect with
        others heading the same way.
      </p>
    </PageLayout>
  );
}
